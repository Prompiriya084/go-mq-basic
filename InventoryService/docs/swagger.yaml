definitions:
  models.Inventory:
    properties:
      product_id:
        type: string
      qty:
        type: integer
    required:
    - product_id
    - qty
    type: object
info:
  contact: {}
paths:
  /api/inventory:
    get:
      consumes:
      - application/json
      description: Get all stocks
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.Inventory'
            type: array
      summary: Get all stocks
      tags:
      - Inventory
swagger: "2.0"
